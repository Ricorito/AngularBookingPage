import { ReszletekDirectiveDirective } from './reszletek.directive';
describe('ReszletekDirective', () => {
  it('should create an instance', () => {
    const directive = new ReszletekDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
